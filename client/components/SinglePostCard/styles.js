import styled from 'styled-components'

export const SinglePostStyles = styled.article`
  background: white;
  padding: var(--spacer);
  max-width: 55rem;
  margin: 0 auto;
  p {
    font-size: 1.7rem;
    font-weight: var(--fw-light);
  }
`
