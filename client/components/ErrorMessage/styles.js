import styled from 'styled-components'

export const ErrorMesssageStyles = styled.div`
  padding: 0.5rem 0;
  p {
    text-align: center;
    font-size: 1.6rem;
    font-weight: var(--fw-bold);
    color: #db2342;
  }
`
